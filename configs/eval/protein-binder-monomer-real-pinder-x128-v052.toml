# 128-rollout hosted PINDER eval for protein-binder-monomer-real v0.5.2.
# Requires hosted secrets/env vars: PROTEIN_BINDER_API_TOKEN and PROTEIN_BINDER_REMOTE_API_BASE_URL.
# Direct launch used for the long run: n=32, rollouts_per_example=4, max_concurrent=2.

[[eval]]
env_id = "d42me/protein-binder-monomer-real@0.5.2"
model = "openai/gpt-4.1-mini"
num_examples = 32
rollouts_per_example = 4
args = { task_library = "pinder", num_eval_examples = 32, max_turns = 30, remote_api_base_url = "http://95.133.252.95:8765", sync_support_on_start = false }
